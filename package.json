{
  "scripts": {
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix Frontend",
    "dev": "concurrently \"npm run backend\" \"wait-on http://localhost:5001 && npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.5"
  }
}